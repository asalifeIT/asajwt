import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.asaservice.beta',
  appName: 'asaservice',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
